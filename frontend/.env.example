# Chatbot frontend (Vite) — API calls in production

# Backend base URL (no trailing slash). Required when the widget is hosted separately from the API.
# VITE_API_URL=https://master-watch-1zeu.vercel.app

# Local dev: leave unset to use Vite proxy (/chat, /api → localhost:3000).
